server:
  port: 8090

resilience4j:
  circuitbreaker:
    instances:
      parameters:
        base-config: default
      operators:
        base-config: default
      users:
        base-config: default