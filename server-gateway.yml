server:
  port: 8090

resilience4j:
  circuitbreaker:
    configs:
      default:
        ignore-exceptions: null
    instances:
      parameters:
        base-config: default
      operators:
        base-config: default
      users:
        base-config: default
  retry:
    instances:
      baseretry:
        ignore-exceptions: null